let WIDTH = 800; // Math.min(800, vw);
let HEIGHT = 500; // Math.min(500, vh);
let GROUND_HEIGHT = 50;
let GOAL_HEIGHT = 5000;
let GOAL_WIDTH = 20;

let BALL_SIZE = 15;
let BALL_MASS = 1;
let firstBot = false;
let secondBot = false;
let GRAVITY = 0.5;
let physicsType = 'sketch';

// Player attributes
let PLAYER_SIZE = 15; // Radius
let PLAYER_SPEED = 0.3;
let PLAYER_JUMP = 10;
let PLAYER_MASS = 8;
